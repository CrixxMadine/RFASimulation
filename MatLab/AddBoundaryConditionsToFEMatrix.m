function [A_h, f_h] = AddBoundaryConditionsToFEMatrix(A_h, f_h, pmesh, tmesh, bmesh);

%% Function summary and arguments description

% Adds the specific boundary conditions for the current system of equations

end

